{
  "name": "atlas",
  "private": true,
  "scripts": {
    "start": "hugo server",
    "build": "hugo && npm run build",
    "build:preview": "hugo --buildDrafts --buildFuture && npm run build",
    "server": "hugo server"
  },
  "dependencies": {
    "autoprefixer": "^9.7.6",
    "netlify-lambda": "^1.6.3",
    "postcss-cli": "^7.1.1"
  },
  "browserlists": [
    "ie >= 8",
    "last 3 versions"
  ]
}
